server {
    listen 80;
    server_name _;

    location / {
        proxy_pass http://{{ hostvars[groups['role_APPLI'][0]]['ansible_host'] }}:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}